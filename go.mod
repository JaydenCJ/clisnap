// clisnap — snapshot testing for CLIs: record command output, redact
// volatility, diff on re-run.
//
// version:    0.1.0
// author:     JaydenCJ
// license:    MIT
// repository: https://github.com/JaydenCJ/clisnap
// keywords:   snapshot-testing, cli, golden-files, redaction, diff, regression-testing, developer-tools
//
// Zero runtime dependencies: the require list below is intentionally empty
// and must stay that way (see CONTRIBUTING.md, "Ground rules").
module github.com/JaydenCJ/clisnap

go 1.22
